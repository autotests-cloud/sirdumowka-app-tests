# multistack_ci — manual (@Tag(manual) — exploratory stubs)
# ./gradlew testManual -Denv=multistack_ci_manual

# Allow attachments after each test
attachBrowserConsoleLogs=false
attachHarLogs=false
attachLastScreenshot=true
attachPageSource=true
attachVideo=false
# Allow allure steps listener
enableAllureSelenideListener=true

# Target app
baseUrl=http://localhost:8820/

# REST API
apiBaseUrl=http://localhost:8820/

# Browser configuration
browser=chrome
browserVersion=148.0
browserSize=1920x1280
headless=false
closeBrowserAfterEach=true
closeBrowserAfterAll=true

# Remote browser hub configuration
enableHar=false
enableVnc=false
enableVideo=false
# videoFolder=
# remoteUrl=

# Console log
logToConsole=true
selenideLogToConsole=true
rootLogLevel=info
